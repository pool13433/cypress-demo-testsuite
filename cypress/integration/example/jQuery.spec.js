describe('ตัวอย่างโค๊ดการ การใช้งาน jQuery ร่วมกับ Cypress', () => {
  context('getJSON', () => {
    /*Cypress.$.getJSON('https://coronavirus-19-api.herokuapp.com/countries').then(res => {
      res.forEach((data, index) => {
        console.log('data ::', data.country)
        it('country Name ::==' + data.country, () => {
          cy.log('name ::==', res.data.country)
        })
      })
    })*/
  })
});